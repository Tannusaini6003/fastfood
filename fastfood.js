//  function initMap(){
//     const map = document.getElementById('map') {
//         zoom: 10,  // Zoom level
//         center: location
//     });

//     // Add a click event listener to the map
//     map.addListener("click", (event) => {
//         alert("You clicked on the map at latitude: " + event.latLng.lat() + ", longitude: " + event.latLng.lng());
//     });
//  }